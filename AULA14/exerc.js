const readlineSync = require('readline-sync');
const readlineSyn = require('readline-sync');

const frase = readlineSync.question("Qual seu nome?");
const frase6 = readlineSyn.question("Qual seu Email?")

console.log(`O seu email ${frase6} fui cadastrado com sucesso ${frase}?`)